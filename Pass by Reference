#include <stdio.h>
#include <conio.h>
void swap(int &x,int &y);

main()
{
 int x,y;
 scanf("%d%d", &x,&y);
 swap(x,y);
 printf("%d %d",x,y);
}
void swap(int &x,int &y)
{
 int temp;
 temp = x;
 x = y;
 y = temp;
}
