#include<stdio.h>
#include<string.h>
int main()
{
	char text[100];
	char* ptr;
	ptr = text;
	scanf("%s" ,text);
	printf(" "); 
	while(*ptr != '\0') ptr++;
	ptr--;
	while(ptr >= text) {
		
		printf("%c", *ptr); 
		ptr--;
	}
	return 0;
}
